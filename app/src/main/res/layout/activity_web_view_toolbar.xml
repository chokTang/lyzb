<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:background="@drawable/bottom_border_one"
    android:contentInsetEnd="0dp"
    android:contentInsetRight="0dp"
    app:contentInsetEnd="0dp"
    app:contentInsetLeft="0dp"
    app:contentInsetRight="0dp"
    app:contentInsetStart="0dp"
    app:navigationIcon="@null"
    >

    <ImageButton
        android:id="@+id/activity_web_view_toolbar_backImageButton"
        style="?attr/borderlessButtonStyle"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@mipmap/btn_back_dark"
        android:tint="@color/colorThree"
        />

    <ImageButton
        android:id="@+id/activity_web_view_toolbar_closeImageButton"
        style="?attr/borderlessButtonStyle"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@mipmap/btn_close_red"
        android:tint="@color/colorThree"
        />

    <TextView
        android:id="@+id/toolbar_common_titleTextView"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
        android:layout_width="match_parent"
        android:layout_height="49dp"
        android:layout_gravity="center"
        android:background="@drawable/activity_web_view_title"
        android:gravity="start|center_vertical"
        android:lines="1"
        android:maxLines="1"
        android:paddingLeft="10dp"
        android:singleLine="true"
        android:text="@string/placeholderActivityTitle"
        android:textColor="@color/colorTwo"
        android:textSize="18sp"
        />

</android.support.v7.widget.Toolbar>